# 🚀 QUICKSTART - Быстрый старт

## ⚡ За 2 минуты до работающего приложения

### 1️⃣ Запуск (30 сек)

```bash
cd "c:\Users\Savely\Documents\Visual Studio 2022\travel\webkademy"
npm run dev
```

✅ Приложение запустится на **http://localhost:3001**

---

### 2️⃣ Вход в систему (30 сек)

#### Вариант A: Обычный пользователь

1. Откройте http://localhost:3001/bus/login
2. Введите:
   - Email: `user@example.com`
   - Password: `password`
3. Нажмите кнопку "Войти"
4. ✅ Вы на странице профиля `/bus/profile`

#### Вариант B: Администратор

1. Откройте http://localhost:3001/bus/login
2. Введите:
   - Email: `admin@example.com`
   - Password: `password`
3. Нажмите кнопку "Войти"
4. Нажмите на профиль (вверху справа) → "Админ-панель"
5. ✅ Вы в админ-панели `/admin/dashboard`

#### Вариант C: Новая регистрация

1. Откройте http://localhost:3001/bus/register
2. Заполните форму:
   - Имя: Ваше имя
   - Email: Ваш email
   - Пароль: Любой пароль (6+ символов)
3. Нажмите "Зарегистрироваться"
4. ✅ Вы будете залогирены и перенаправлены в профиль

---

### 3️⃣ Основные маршруты (1 мин)

| Маршрут             | Описание                  | Link                                   |
| ------------------- | ------------------------- | -------------------------------------- |
| `/`                 | Главная страница          | http://localhost:3001                  |
| `/bus`              | Страница автобусов        | http://localhost:3001/bus              |
| `/bus/search`       | Поиск рейсов              | http://localhost:3001/bus/search       |
| `/bus/schedule/1`   | Информация о рейсе        | http://localhost:3001/bus/schedule/1   |
| `/bus/login`        | Вход в систему            | http://localhost:3001/bus/login        |
| `/bus/register`     | Регистрация               | http://localhost:3001/bus/register     |
| `/bus/profile`      | Личный кабинет            | http://localhost:3001/bus/profile      |
| `/admin/dashboard`  | Админ-панель              | http://localhost:3001/admin/dashboard  |
| `/admin/schedules`  | Управление рейсами        | http://localhost:3001/admin/schedules  |
| `/admin/users`      | Управление пользователями | http://localhost:3001/admin/users      |
| `/admin/bookings`   | Управление бронированиями | http://localhost:3001/admin/bookings   |
| `/admin/statistics` | Статистика                | http://localhost:3001/admin/statistics |

---

## 📝 Команды проекта

```bash
npm run dev           # Запуск dev сервера
npm run build         # Собрать для продакшна
npm run start         # Запустить продакшн сборку
npm run format        # Форматировать код Prettier
npm run lint          # Проверить код на ошибки
```

---

## 🎯 Что проверить в первую очередь

### ✅ Авторизация

- [ ] Перейти на `/bus/login`
- [ ] Залогиться как `admin@example.com`
- [ ] Вверху справа видишь профиль
- [ ] Нажать на профиль → видишь меню с "Выход"

### ✅ Админ-панель

- [ ] Нажать на профиль → "Админ-панель"
- [ ] Видишь Dashboard с статистикой
- [ ] Перейти на "Schedules", "Users", "Bookings", "Statistics"
- [ ] Все таблицы и данные отображаются

### ✅ Поиск рейсов

- [ ] Вернись на главную `/bus`
- [ ] Заполни форму поиска (город, дату)
- [ ] Нажми кнопку поиска
- [ ] Увидишь результаты на `/bus/search`

### ✅ Адаптивность

- [ ] Открой Dev Tools (F12)
- [ ] Переключись в режим телефона (iPhone X)
- [ ] Проверь что всё работает на мобиле
- [ ] Вернись на десктоп

---

## 🎨 Темные места в коде (для изучения)

### Авторизация - `contexts/AuthContext.tsx`

```typescript
// Как использовать в компоненте:
const { user, login, logout, isAuthenticated } = useAuth();

// Пример входа:
await login("user@example.com", "password");

// Пример выхода:
await logout();
```

### Header с интеграцией Auth - `components/bus/header.tsx`

```typescript
// Как показывается меню в зависимости от авторизации:
{isAuthenticated && user ? (
  // Меню для залогиненного пользователя
) : (
  // Кнопки входа и регистрации
)}
```

### AdminPanel - `app/admin/dashboard/page.tsx`

```typescript
// Как отображается статистика:
const stats = [
  { label: 'Всего бронирований', value: '428' },
  { label: 'Активные рейсы', value: '24' },
  // ...
];

{stats.map(stat => (...))}
```

---

## 🚨 Если что-то не работает

### Проблема: Порт уже занят

```
⚠ Port 3000 is in use, using available port 3001 instead.
```

✅ Это нормально, приложение запустится на port 3001

### Проблема: "Unable to acquire lock"

```
⨯ Unable to acquire lock at .next/dev/lock
```

**Решение:**

```bash
# Закончи предыдущий dev сервер (Ctrl+C)
# Или удали папку .next и запусти снова
rm -r .next
npm run dev
```

### Проблема: Форматирование не работает

```bash
npm run format
```

### Проблема: Зависимости не установлены

```bash
rm -r node_modules package-lock.json
npm install
npm run dev
```

---

## 📚 Дополнительная документация

Прочитай эти файлы для глубокого понимания:

1. **COMPLETION_CHECKLIST.md** - Что было сделано (в этой папке)
2. **PROJECT_OVERVIEW.md** - Полный обзор проекта
3. **AUTH_GUIDE.md** - Подробная документация авторизации
4. **DEVELOPMENT_RECOMMENDATIONS.md** - План развития проекта

---

## 💡 Советы для разработки

### Быстро добавить новый компонент:

```bash
# Создай файл в components/bus/MyComponent.tsx
# Используй как:
import MyComponent from '@/components/bus/MyComponent';
```

### Добавить новую страницу:

```bash
# Создай папку app/bus/new-page/
# И файл page.tsx внутри
# Страница будет доступна на /bus/new-page
```

### Использовать AuthContext:

```typescript
"use client";
import { useAuth } from "@/contexts/AuthContext";

const { user, isAuthenticated } = useAuth();
```

### Стилизовать компонент:

```typescript
// Используй Tailwind классы:
<div className="bg-blue-600 text-white rounded-lg p-4">
  Hello World
</div>
```

---

## 🎉 Готово!

**Приложение готово к использованию.**

Если у тебя есть вопросы по коду, обратись к документации или исследуй компоненты в VS Code.

---

**Версия:** 1.0  
**Статус:** ✅ Рабочее  
**Последнее обновление:** 2024-01-15  
**Автор:** GitHub Copilot
